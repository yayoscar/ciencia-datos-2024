try:
  base = float(input("ingrese la base: "))
  altura = float(input("ingrese la altura: "))
  area = base * altura / 2

  if base == altura
      exit("el programa solo calcula a rectangulos")

  print(f"el area del rectangulo es,{area}")
except ValueError:
    print("hubo un herror,intentalo de nuevo")