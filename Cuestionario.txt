
¿Qué hace el comando git init y en qué situaciones se utiliza? Se utiliza para inicializar un nuevo repositorio de Git en un directorio, contiene toda la información de configuración y los archivos necesarios para el  control.


¿Cuál es la diferencia entre git add y git commit en el flujo de trabajo de Git? Git Add prepara los cambios en los archivos y no se agregan automáticamente al historial. Git commit registra los cambios en el repositorio creando algún punto en el historial de proyecto.


¿Qué significa que un archivo esté en estado "staged" en Git y cómo se coloca en este estado? significa que el archivo se preparo para ser incluido en el próximo commit y Git ah registrado el archivo y se preparo para ser listo en el historial del reositorio.


¿Cuál es la diferencia entre git reset --soft, git reset --mixed y git reset --hard?
--soft descarta el último commit, --mix descarta el último commit y add, --hard descarta el último commit, add y cualquier cambio que se hagan en los códigos.

¿Qué es una fusión (merge) en Git y cuáles son los posibles conflictos que pueden ocurrir durante el proceso?ocurre cuando alguien de un miembros de un equipo hace cambios conflictivos en el mismo archivo desde dos ramas diferentes.


¿Para qué se usa el comando git checkout y cuál es su diferencia con git switch? git checkout se utiliza tanto para cambiar ramas como para restaurar estados de archivos, mientras que git switch está diseñado específicamente para cambiar ramas.


¿Cómo funciona git pull y en qué se diferencia de git fetch?git fetch recupera los cambios del repositorio remoto sin aplicarlos a la rama local, ejecutar git pull integra automÃ¡ticamente los cambios del repositorio remoto en la rama local.


¿Qué ocurre cuando se ejecuta git push y qué problemas pueden surgir si hay cambios en el repositorio remoto?El comando git push sube el contenido de un repositorio local a un repositorio central.


El comando git push sube el contenido de un repositorio local a un repositorio central.El comando git push te permite subir los commits desde tu rama (branch) local en tu repositorio git local al repositorio remoto. 
Flujo de trabajo en Git

