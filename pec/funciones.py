
def comparar_precios(producto, p1, p2, p3, cantidad):
    p1 = float(p1)
    p2 = float(p2)
    p3 = float(p3)
    cantidad = int(cantidad)

    total1 = p1 * cantidad
    total2 = p2 * cantidad
    total3 = p3 * cantidad

    totales = [total1, total2, total3]
    return resultado

#ME FALTA TERMINAR